// QVAC SDK config, loaded at runtime init (point at it via QVAC_CONFIG_PATH if needed).
// For a same-LAN demo no relays are required. For cross-internet (roadmap), add
// Hyperswarm blind-relay public keys here so peers behind NAT/firewalls can connect.
export default {
  swarmRelays: [],
};
