node_modules/
dist/
electron/dist/
dist-engine/
release/
*.log

# hardhat (contracts/) build output — keep the .sol sources + tests, drop generated artifacts
contracts/artifacts/
contracts/cache/
contracts/typechain-types/

# secrets — never commit
.env
.env.local
CONDUIT-PLAN.md

# live audit logs (keep AUDIT_LOG.sample.jsonl as a committed example)
AUDIT_LOG.jsonl
audit-*.jsonl

# QVAC / model caches
.qvac/
models/
*.gguf

# os / editor
.DS_Store
.idea/
.vscode/
