{
  "name": "conduit-subgraph",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Global seller settlement history for Conduit, indexed from ConduitEscrow.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:sepolia": "graph build --network sepolia",
    "build:arc": "graph build --network arc-testnet",
    "auth": "graph auth",
    "deploy:sepolia": "graph deploy conduit-settlement-sepolia --network sepolia",
    "deploy:arc": "graph deploy conduit-settlement-arc-testnet --network arc-testnet"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  }
}
